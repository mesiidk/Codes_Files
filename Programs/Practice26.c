#include <stdio.h>

int main() {
    char cha;
    printf("Enter a character:\n");
    scanf("%c", &cha);
    short ch;
    printf("Enter a signed short value:");
    scanf("%hd", &ch);

    return 0;
}
